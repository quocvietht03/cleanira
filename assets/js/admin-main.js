!(function($){
	"use strict";

	jQuery(document).ready(function($) {

	});

	jQuery(window).on('resize', function() {
    CleaniraSubmenuAuto();
	});

	jQuery(window).on('scroll', function() {

	});

})(jQuery);
